// Ex2 Reserved Keyword:
const RESERVED_KEYWORDS = ['break', 'case', 'catch', 'class', 'const', 'continue',
  'debugger', 'default', 'delete', 'do', 'else', 'enum', 'export', 'extends', 'finally',
  'for', 'function', 'if', 'implements', 'import', 'in', 'instanceof', 'interface',
  'let', 'new', 'package', 'private', 'protected', 'public', 'return', 'static',
  'super', 'switch', 'this', 'throw', 'try', 'typeof', 'var', 'void', 'while',
  'with', 'yield'];

function isReserved(name) {
  for (let idx = 0; idx < RESERVED_KEYWORDS.length; idx++) {
    let reserved = RESERVED_KEYWORDS[idx];

    if (name === reserved) return true;
    }

  return false;
}

console.log(isReserved('monkey')); // false
console.log(isReserved('patch'));  // false
console.log(isReserved('switch')); // should be: true

/* Explanation: for each cannot return anything, therefore the return statement
inside of it does nothing and the return false statement is being executed
everytime after the forEach statement has finished running.*/
